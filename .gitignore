/target
/data
/logs

# Secrets. antenna.toml carries handoff_secret, inspect_token and the path to
# the GitHub App private key; it must never be committed.
antenna.toml
antenna.toml.bak-*
*.pem
*.key
.env
.env.*

# Local noise
.DS_Store
edge/github-ingress/node_modules
edge/github-ingress/.wrangler

# Compiled binary: a build output, not source.
/bin/antenna
runtime/.venv/
__pycache__/
*.pyc
